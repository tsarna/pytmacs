$Id: TODO,v 1.44 2007-09-27 01:24:36 tsarna Exp $

modes
minibuffer:
    disable window commands, save, quit

view:
    kill/copy/yank

termioscap:
    decode_and_send: split multiple chars into separate queues
    timeout at the c level (make cb an attr and clear it)
    implement doRead at the C level

tcui:
    real buffer display
    resize display
    grow/shrink/resize
    why doesn't esc prompt print?
    status line
        	
baseui:
    macros
    quickexit
    bindkey unbindkey describe_bindings
    doc strings - keys
    
ubuf:
    _new?
    textarg		from ubuf
    .borrow()		with args
    dealloc		point uobj to full memory
    efficient grow
    writelines
    file-like tests from Buffer
    doc strings

marker:
    searching
    _new?
    call
    file:
	readlines()
	encoding?
    movement:
	para, fence
    searching:
    editing:
	trimline
	twiddle
    auto-adjust on edits (colseek!)
    changed flag
    nonzero -- returns changed flag?
    doc strings
	
Buffer:
	tests:
		.save()
		display name, filename

	.filter()
	.load()
	redo write_file based on iterator of buffer
	del_buffer()
	.set()
	rework sniff based on set
	
	illegal-attributes:
		closed
		softspace
		display_name

sniff:
	regex
	postSniff
	safe eval?
	
implement tclayer write w/ unicode encode

write keysyms repr doctests
write termioscap description
