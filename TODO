$Id: TODO,v 1.25 2007-08-29 13:58:25 tsarna Exp $

ubuf:
    textarg		from ubuf
    .borrow()		with args
    dealloc		point uobj to full memory
    efficient grow
    writelines
    file-like tests from Buffer

marker:
    call
    file:
	readlines()
	encoding?
    movement:
	toline
	nextline
	prevline
    searching:
    editing:
	        
    auto-adjust on edits
    changed flag
    nonzero -- returns changed flag?
	
Buffer:
	tests:
		.save()
		display name, filename

	.filter()
	.load()
	redo write_file based on iterator of buffer
	del_buffer()
	.set()
	rework sniff based on set
	
	illegal-attributes:
		closed
		softspace
		display_name

sniff:
	regex
	postSniff
	safe eval?
	
implement tclayer write w/ unicode encode

write keysyms repr doctests
write termioscap description


