$Id: TODO,v 1.57 2007-10-10 15:20:30 tsarna Exp $

termioscap:
    implement tclayer write w/ unicode encode

baseui:
    bindkey unbindkey describe_bindings
    
ubuf:
    efficient grow
    writelines
    file-like tests from Buffer

marker:
    marker.killregion(killbuf[, end])
    marker.copyregion(killbuf[, end])

    call?
    file:
	readlines()
	encoding?
    movement:
	para, fence
    searching:
    editing:
	trimline
	twiddle
	tab
    nonzero -- returns changed flag?
	
Buffer:
	tests:
		.save()
		display name, filename

	.filter()
	.load()
	redo write_file based on iterator of buffer
	del_buffer()

sniff:
	regex
	postSniff
	safe eval?
	

