$Id: TODO,v 1.54 2007-10-04 16:56:17 tsarna Exp $

termioscap:
    implement tclayer write w/ unicode encode

baseui:
    bindkey unbindkey describe_bindings
    
ubuf:
    efficient grow
    writelines
    file-like tests from Buffer

marker:
    killline
    killregion
    copyregion
	ubuf_killcopy(ubuf *d, ubuf *s, start, end, copy, append)

    call?
    file:
	readlines()
	encoding?
    movement:
	para, fence
    searching:
    editing:
	trimline
	twiddle
	tab
    nonzero -- returns changed flag?
	
Buffer:
	tests:
		.save()
		display name, filename

	.filter()
	.load()
	redo write_file based on iterator of buffer
	del_buffer()

sniff:
	regex
	postSniff
	safe eval?
	

